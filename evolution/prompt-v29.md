# Prompt V29

Complete the reviewed S05 runtime trust-boundary hardening without weakening
approved advertising compatibility or activating an external system:

- apply one special-address, DNS-rebinding, redirect, and injected-client
  policy to configurable bidder and callback traffic;
- inventory every first-party creative consumer, keep executable Banner HTML
  in one opaque-origin browser container, and define the gate for a future
  native renderer;
- replace caller-shaped HTTP/CLI actor state with exact session-bound request
  principals and restricted operating-system maintenance identities;
- preserve established Active quality behavior while newer Observe/Canary
  versions coexist; and
- protect enforcement and billing identity columns while retaining every
  legitimate rollback, independent review, and Hold application transition.
