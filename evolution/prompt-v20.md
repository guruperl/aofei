# Prompt V20

Make the public/private repository boundary enforceable after a security audit
found active SMTP credentials in reachable history and production-derived
accounts, traffic, uploads, and deployment paths in tracked artifacts.

- contain the live credential before history work;
- keep production data and customer source documents outside Git;
- preserve reproducible local development with deterministic synthetic data;
- rewrite every public ref in both repositories; and
- add CI/local leak gates that inspect current files and complete history.
